# 开发说明

## 1. 当前项目状态

仓库已经包含：

- 产品需求文档
- 技术架构文档
- Spring Boot 后端基础骨架
- 核心业务接口雏形
- MySQL 初始化脚本
- Vue 3 管理后台基础工程
- 微信小程序基础工程

当前后端接口已覆盖：

- 首页接口
- 菜品分类与列表
- 点餐下单与订单查询
- 客房扫码解析
- 包间列表、时段查询、预约提交
- 宴席预约提交与列表
- 后台登录
- 后台订单、包间预约、宴席管理列表
- 后台订单、包间预约、宴席状态更新
- 后台订单详情与明细查看
- 后台订单按状态与场景筛选
- 后台订单场景中文标签展示
- 后台订单与预约金额、日期格式统一展示
- 后台订单与预约支持本地关键字快速搜索
- 后台菜品管理支持关键字搜索与分类筛选
- 后台订单、菜品、预约列表支持结果计数与空状态提示
- 后台统计卡片可跟随订单与菜品筛选结果变化
- 后台分类支持关键字搜索，订单支持一键复制联系电话
- 后台包间预约与宴席预约支持一键复制联系电话
- 后台包间预约按预约状态筛选
- 后台宴席预约按状态筛选
- 后台宴席预约列表展示联系电话与预算
- 后台菜品列表、保存、上下架
- 后台菜品分类列表、保存、启停
- 后台包间预约详情与宴席预约详情查看
- 后台包间预约展示包间名称与中文时段
- 后台宴席跟进记录新增与查看
- 后台状态标签优化与宴席跟进时间展示

当前数据访问层状态：

- 已接入 MyBatis-Plus
- 已补充核心实体与 Mapper
- 当前下单、客房扫码、包间预约、宴席预约已按数据库方式组织代码
- 已补充后台管理员登录与 JWT 基础能力
- 仍未接入微信登录、JWT 实鉴权、微信支付

## 2. 本地运行要求

后端要求：

- JDK 17
- Maven 3.9+
- MySQL 8
- Redis 6+

说明：

- 当前项目使用 `Spring Boot 3`
- `Spring Boot 3` 最低要求 `Java 17`
- 如果本机仍是 `Java 8`，项目无法正常编译

## 3. 当前环境已发现的问题

本机当前 `java -version` 为 `1.8`

因此执行 `mvn test` 时会报错：

- 类文件版本为 `61.0`
- 当前运行环境版本为 `52.0`

对应关系：

- `61.0` = Java 17
- `52.0` = Java 8

## 4. 推荐启动方式

### 4.1 切换到 JDK 17

确保 `JAVA_HOME` 指向 JDK 17，并重新打开终端。

### 4.2 初始化数据库

执行：

```sql
source backend/src/main/resources/db/schema.sql;
```

### 4.3 启动后端

在 `backend/` 目录执行：

```bash
mvn spring-boot:run
```

### 4.4 打开接口文档

启动成功后访问：

- `/swagger-ui.html`

### 4.5 默认后台账号

数据库初始化后默认账号：

- 用户名：`admin`
- 密码：`admin123`

### 4.6 启动后台前端

在 `admin/` 目录执行：

```bash
npm install
npm run dev
```

默认访问：

- `http://localhost:5173`

说明：

- 后台前端已通过 Vite 代理转发 `/api` 和 `/admin` 请求到 `http://localhost:8080`
- 本地开发时通常不需要再手动改前端接口地址
- 后台当前已支持订单、菜品、包间预约、宴席预约四个管理入口
- 后台当前已支持分类管理与菜品管理联动

### 4.7 打开小程序工程

在微信开发者工具中导入目录：

- `/Users/wei/code/weixin/miniprogram`

当前已提供页面：

- 首页
- 点餐页
- 购物车
- 我的
- 客房扫码点餐页
- 下单结果页
- 我的订单页
- 订单详情页
- 订单确认页
- 我的预约页
- 预约详情页
- 包间预约页
- 宴席预约页

当前下单链路已支持：

- 购物车进入确认页
- 确认页展示菜品金额、配送费、应付金额
- 下单成功后可直接跳转到“我的订单”

## 5. 下一阶段开发建议

建议按下面顺序继续：

1. 接入 MyBatis-Plus 实体和 Mapper
2. 完善 DTO/VO 与统一分页结构
3. 完成微信登录
4. 完善后台登录态与权限模型
5. 接入微信支付
6. 补后台管理接口
7. 开始小程序正式页面开发
